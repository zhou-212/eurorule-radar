---
/**
 * 旧 /promote/* 路径的 301 redirect 兜底
 *   - /promote 内部 operator 页,无子页面
 *   - catch-all 兜底任意子路径
 */
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  return [];
}

const slug = Astro.params.slug;
return Astro.redirect(`/eu/promote/${slug}/`, 301);
---
